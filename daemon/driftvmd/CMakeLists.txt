AUX_SOURCE_DIRECTORY(. SRCFILES)
AUX_SOURCE_DIRECTORY(./rpc RPCFILES)
AUX_SOURCE_DIRECTORY(./rpc/base64 B64FILES)
add_executable (driftvmd ${SRCFILES} ${RPCFILES} ${B64FILES})
TARGET_LINK_LIBRARIES(driftvmd ${LIBS})
